import React from 'react';
import { HeaderWrapper } from './styles';
import Logo from '../Logo'

const Header = () =>{
  return (
    <HeaderWrapper>
      <Logo />
    </HeaderWrapper>
  );
};
export default Header;
