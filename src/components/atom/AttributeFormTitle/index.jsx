import styled from 'styled-components';
import FormTitle from '../FormTitle';

export default styled(FormTitle)`
  margin-left: auto;
  margin-right: auto;
  width: 50%;
  color: black;
  background-color: white;
`;